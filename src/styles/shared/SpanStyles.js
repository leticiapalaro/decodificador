import styled from 'styled-components'

const SpanStylesParagrafo = styled.span`
  font-size: 1.2rem;
  font-weight: bold;
  color: var(--azul-padrao);
  text-transform: uppercase;
`

export default SpanStylesParagrafo;